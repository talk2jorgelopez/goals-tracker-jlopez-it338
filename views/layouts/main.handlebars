<!DOCTYPE html>

<html lang="en">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Goals tracking app</title>
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Raleway">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" type="text/css" href="public/css/main.css" />

    <style>
	body,h1,h2,h3,h4,h5 {font-family: "Raleway", sans-serif}
	.w3-third img{margin-bottom: -6px; opacity: 0.8; cursor: pointer}
	.w3-third img:hover{opacity: 1}
    </style>

<body>

<!-- Sidebar (hidden by default) -->
<nav class="w3-sidebar w3-bar-block w3-card-2 w3-top w3-xlarge w3-animate-left" style="display:none;z-index:2;width:40%;min-width:300px" id="theSidebar">
  <a href="javascript:void(0)" onclick="w3_close()"
  class="w3-bar-item w3-button">Close Menu</a>
  <a href="#goals" onclick="w3_close()" class="w3-bar-item w3-button">Goals</a>
</nav>

<!-- Top menu -->
<div class="w3-top">
  <div class="w3-white w3-xlarge" style="max-width:1200px">
    <div class="w3-button w3-padding-16 w3-left" onclick="w3_open()">☰</div>
    <div class="w3-center w3-padding-16">My Goals</div>
  </div>
</div>


<div class="w3-main" style="max-width:1200px;margin-top:100px">
{{{body}}}

</div>

<!-- End page content -->
</div>

<script defer src="https://code.getmdl.io/1.1.3/material.min.js"></script>
<script src="https://cdn.jsdelivr.net/umbrella/2.0.1/umbrella.min.js"></script>
<script type="text/javascript">
// Script to open and close sidebar
function w3_open() {
    document.getElementById("theSidebar").style.display = "block";
}

function w3_close() {
    document.getElementById("theSidebar").style.display = "none";
}

// domReady - Credit: beeker.io
var domReady = function(callback) {
	document.readyState === "interactive" || document.readyState === "complete" ? callback() : document.addEventListener("DOMContentLoaded", callback);
        };
        domReady(function() {
            var deleteButtons = u('.delete');
            deleteButtons.each(function(currentValue) {
                var li = currentValue.closest('li');
                var id = li.id;
                currentValue.addEventListener('click', function() {
                    fetch('goals/' + id, {
                        method: 'delete'
                    }).then(function(responseObj) {
                        li.remove();
                    });
                });
            });
        });
</script>

</body>

</html>
